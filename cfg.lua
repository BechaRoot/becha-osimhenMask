cfg = {
    unequipCommand = 'unequipMask',
    itemName = 'osimhenmask',
}
-- For standalone usage;
-- TriggerEvent('sh-osimhenMask:client:wearMask') | Client Event
-- For unequip chain;
-- TriggerEvent('sh-osimhenMask:client:unequipMask') | Client Event
